# STRIP irá remover espaços inuteis no inicio e no final da str

frase = '   pão de batata   '
print(frase.strip())

# dentro de STRIP, temos o RSTRIP, que apaga os espaços inuteis só da direita
# e o LSTRIP, que apaga os espaços inuteis da esquerda.